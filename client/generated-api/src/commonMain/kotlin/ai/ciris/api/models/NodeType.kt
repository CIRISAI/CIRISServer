/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 * NOTE (CIRISServer): hand-extended beyond the OpenAPI spec to carry the CEG trust-root
 * node kinds projected by the server's `seed_ceg_graph` (owner / owned_node /
 * canonical_server / family / holder / delegation / attestation / peer) and to
 * deserialize TOLERANTLY — an unrecognised `type` decodes to `UNKNOWN` instead of
 * throwing, so one new server-side node kind can never crash the whole graph load.
 * Keep these in sync with `src/memory_api.rs::seed_ceg_graph`.
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.ciris.api.models


import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * Types of nodes in the graph.
 *
 * Values: AGENT,USER,CHANNEL,CONCEPT,CONFIG,TSDB_DATA,TSDB_SUMMARY,CONVERSATION_SUMMARY,TRACE_SUMMARY,AUDIT_SUMMARY,TASK_SUMMARY,AUDIT_ENTRY,IDENTITY_SNAPSHOT,BEHAVIORAL,SOCIAL,IDENTITY,OBSERVATION,CONSENT,DECAY,MODERATION,SAFETY_SCORE,OWNER,OWNED_NODE,CANONICAL_SERVER,FAMILY,HOLDER,DELEGATION,ATTESTATION,PEER,UNKNOWN
 */
@Serializable(with = NodeTypeSerializer::class)
enum class NodeType(val value: kotlin.String) {

    @SerialName(value = "agent")
    AGENT("agent"),

    @SerialName(value = "user")
    USER("user"),

    @SerialName(value = "channel")
    CHANNEL("channel"),

    @SerialName(value = "concept")
    CONCEPT("concept"),

    @SerialName(value = "config")
    CONFIG("config"),

    @SerialName(value = "tsdb_data")
    TSDB_DATA("tsdb_data"),

    @SerialName(value = "tsdb_summary")
    TSDB_SUMMARY("tsdb_summary"),

    @SerialName(value = "conversation_summary")
    CONVERSATION_SUMMARY("conversation_summary"),

    @SerialName(value = "trace_summary")
    TRACE_SUMMARY("trace_summary"),

    @SerialName(value = "audit_summary")
    AUDIT_SUMMARY("audit_summary"),

    @SerialName(value = "task_summary")
    TASK_SUMMARY("task_summary"),

    @SerialName(value = "audit_entry")
    AUDIT_ENTRY("audit_entry"),

    @SerialName(value = "identity_snapshot")
    IDENTITY_SNAPSHOT("identity_snapshot"),

    @SerialName(value = "behavioral")
    BEHAVIORAL("behavioral"),

    @SerialName(value = "social")
    SOCIAL("social"),

    @SerialName(value = "identity")
    IDENTITY("identity"),

    @SerialName(value = "observation")
    OBSERVATION("observation"),

    @SerialName(value = "consent")
    CONSENT("consent"),

    @SerialName(value = "decay")
    DECAY("decay"),

    @SerialName(value = "moderation")
    MODERATION("moderation"),

    @SerialName(value = "safety_score")
    SAFETY_SCORE("safety_score"),

    // ─── CEG trust-root kinds (server seed_ceg_graph) — hand-extended, keep in sync ───

    @SerialName(value = "owner")
    OWNER("owner"),

    @SerialName(value = "owned_node")
    OWNED_NODE("owned_node"),

    @SerialName(value = "canonical_server")
    CANONICAL_SERVER("canonical_server"),

    @SerialName(value = "family")
    FAMILY("family"),

    @SerialName(value = "holder")
    HOLDER("holder"),

    @SerialName(value = "delegation")
    DELEGATION("delegation"),

    @SerialName(value = "attestation")
    ATTESTATION("attestation"),

    @SerialName(value = "peer")
    PEER("peer"),

    /** Fallback for any `type` the client doesn't recognise — keeps the graph loadable. */
    @SerialName(value = "unknown")
    UNKNOWN("unknown");

    /**
     * Override [toString()] to avoid using the enum variable name as the value, and instead use
     * the actual value defined in the API spec file.
     *
     * This solves a problem when the variable name and its value are different, and ensures that
     * the client sends the correct enum values to the server always.
     */
    override fun toString(): kotlin.String = value

    companion object {
        /**
         * Converts the provided [data] to a [String] on success, null otherwise.
         */
        fun encode(data: kotlin.Any?): kotlin.String? = if (data is NodeType) "$data" else null

        /**
         * Returns a valid [NodeType] for [data], null otherwise.
         */
        fun decode(data: kotlin.Any?): NodeType? = data?.let {
          val normalizedData = "$it".lowercase()
          values().firstOrNull { value ->
            it == value || normalizedData == "$value".lowercase()
          }
        }
    }
}

/**
 * Tolerant serializer: encodes the wire [NodeType.value] and decodes an unrecognised
 * value to [NodeType.UNKNOWN] rather than throwing. A new server-side CEG node kind
 * then renders as a generic node instead of failing the entire timeline deserialization.
 */
object NodeTypeSerializer : KSerializer<NodeType> {
    override val descriptor: SerialDescriptor =
        PrimitiveSerialDescriptor("ai.ciris.api.models.NodeType", PrimitiveKind.STRING)

    override fun serialize(encoder: Encoder, value: NodeType) {
        encoder.encodeString(value.value)
    }

    override fun deserialize(decoder: Decoder): NodeType {
        val raw = decoder.decodeString()
        return NodeType.decode(raw) ?: NodeType.UNKNOWN
    }
}
